name: NoDeathOnTp
main: me.tabinol.nodeathontp.NoDeathOnTp
version: ${project.version}-${build.number}
permissions:
    nodeathontp.notify:
        description: Notify for death cancellation
        default: op
